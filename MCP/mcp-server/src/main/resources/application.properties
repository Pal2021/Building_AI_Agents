# Fails startup if any property key is unrecognized
spring.config.on-not-found=fail
# ? REQUIRED ? tells Spring AI to expose tools over SSE transport
spring.ai.mcp.server.transport=SSE
spring.ai.mcp.server.name=my-mcp-server
spring.ai.mcp.server.version=1.0.0

